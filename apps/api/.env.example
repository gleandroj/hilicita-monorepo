# Database (PostgreSQL with pgvector)
DATABASE_URL="postgresql://user:password@localhost:5432/hilicita"

# Redis (for document ingest queue)
REDIS_URL="redis://localhost:6379"

# Better Auth
BETTER_AUTH_SECRET="generate-a-long-random-secret"
BETTER_AUTH_TRUSTED_ORIGINS="http://localhost:3000"

# Google SSO (optional; set redirect URI in Google Cloud Console to https://<api-host>/api/auth/callback/google, e.g. http://localhost:4000/api/auth/callback/google)
# GOOGLE_CLIENT_ID="..."
# GOOGLE_CLIENT_SECRET="..."

# CORS (comma-separated origins)
CORS_ORIGIN="http://localhost:3000"

# OpenAI (embeddings + checklist LLM)
OPENAI_API_KEY="sk-..."

# MinIO (S3-compatible storage for uploaded PDFs/CSVs)
MINIO_ENDPOINT="http://localhost:9000"
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"
MINIO_BUCKET="documents"
# MINIO_REGION="us-east-1"

# Server port (default: 4000)
# PORT=4000
